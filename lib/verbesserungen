Nächste Besprechnung:

Änderungen in User Profile und Travel Profile(copy delete modify)
Änderungen in Hauptmenü bzw in der Routeneingabe 

Änderungen in der Favoriten und Letzte Ziele Datenbank, wie wird das alles berechnet
Soll in dieser auch noch der Favorit oder Letzte Ziele auswählbar sein

Ansprechen von Future Builder, da:
- Spätestens sobald die Routenausgabe geladen wird, müssen ALLE DatenbankTabellen geladen werden(vermutlich so gegen 4 o 5)
- ALLE Provider Klassen bzw deren Objekte müssen geladen werden bzw mit der Datenbank gefüllt werden
- (Hoffentlich) muss Google Maps mit der Routenberechnung beginnen, braucht ZEIT!
- Die RandomFunktion zur zufälligen Festlegung von automatisierten Segmenten muss ausgeführt werden 
- und natürlichd der einfache Aufbau aller Widgets auf dieser Seite 
-> FutureBuilder zeigt, solange die Daten noch nicht vollstädnig sind ein alternatives Widget (bsp.: Ladeicon), 
   sobald alle Ladevorgänge abgeschlossen, wird die fertige Seite angezeigt. Dafür die RouteOverview oder wie das heist als 
   Future beschreiben 
-> Problem: Hinzufügen von automatisierten Segmenten in Overview triggert Routenberechnung erneut. Kann noch nicht abschätzen
            infiefern das ein Problem darstellen kann  